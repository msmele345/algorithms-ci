---
platform: linux

image_resource:
  type: docker-image
  source: { repository: maven, tag: 3.5.0-jdk-8-alpine }

#Each task is a running container that needs a base image and inputs
#task inputs can be dependencies like project gits
#OR
#scripts
inputs:
  - name: algorithm-api-git
#  - name: algorithm-ci-git
  - name: task-scripts

#outputs:
#  - name: build

#caches:
#  - path: .gradle/

run:
  path: echo
  args: [cleaned algorithm-api successfully]
#  path: ./task-scripts/build.sh
#  path: algorithm-ci-git/tasks/build/build.sh